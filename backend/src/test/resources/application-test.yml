spring:
  datasource:
    url: jdbc:h2:mem:rickmorty;MODE=PostgreSQL;DATABASE_TO_LOWER=TRUE;DEFAULT_NULL_ORDERING=HIGH
    username: sa
    password:
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: validate
  kafka:
    listener:
      auto-startup: false
  neo4j:
    uri: bolt://localhost:17687
    authentication:
      username: neo4j
      password: test-password
app:
  outbox:
    enabled: false
  auth:
    admin-username:
    admin-password:
